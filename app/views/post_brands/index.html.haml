= render 'posts/top__header'
.post__brand--contents
  .post__brand--contents__box
    %h1 ブランド一覧
    - @post_brands.each do |post_brand|
      %p
        = link_to post_brand_path(post_brand.id) do
          = post_brand.name

= render 'posts/top__banner'
= render 'posts/top__footer'