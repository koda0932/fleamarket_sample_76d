= render 'top__header'
= render 'top__contents'
= render 'top__main'
= render 'top__banner'
= render 'top__footer'




-# 次の実装をするため

- @posts.each do |post|
  = post.name
  = link_to '詳細', post_path(post)
  = link_to '編集', edit_post_path(post)
  = link_to '削除', post_path(post), method: :delete
